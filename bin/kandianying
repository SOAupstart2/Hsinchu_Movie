#!/usr/bin/env ruby
require_relative '../lib/kandianying'

if ARGV[0] == 'vieshow'
	theater = HsinChuMovie::Vieshow.new(ARGV[1].to_s)
	theater.movie_table.each do |movie,time| 
		puts movie
		time.each { |k,v| puts k+"\t"+v }
		puts
	end
end